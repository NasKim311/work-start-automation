# TODO

DeskReady 기능/UX 개선 후보 목록 (2026-07-31 정리, 2026-08-01 갱신, 2026-08-08 신규
후보 추가). 우선순위별로 정리했고, 아래 "다음 개선 후보" 절 이전 항목은 모두 구현
완료됐습니다.

## 🔴 임팩트 크고 구현 부담 적은 것

1. ✅ **삭제 확인이 없음** — `TaskList.tsx`의 삭제(🗑) 버튼은 클릭 즉시 삭제, 되돌릴 방법이 없음.
   → 삭제 전 `window.confirm` 확인 절차 추가.
2. ✅ **저장 안 한 변경사항이 조용히 사라짐** — 작업 추가/수정/순서변경은 React state에만 있고,
   "설정 저장" 버튼을 안 누르고 창을 닫으면 그냥 소실됨.
   → 창 닫기 전 저장 여부를 묻는 네이티브 다이얼로그 추가.
3. ✅ **실행 에러가 사용자에게 전혀 안 보임** — `main.js`의 `run-tasks`는 실패를 `console.error`로만
   남기고, 프로덕션에서는 devtools 없이 확인 불가.
   → 실행 실패를 렌더러로 전달해 alert로 표시.
4. ✅ **개별 작업 테스트 실행 버튼이 없음** — URL/프로그램 경로 확인하려면 전체 목록을 처음부터
   돌려서 누적 딜레이만큼 기다려야 함.
   → 각 항목에 딜레이 없이 즉시 실행하는 "테스트 실행" 버튼 추가.
5. ✅ **"출근 시작하기"에 아무 피드백이 없음** — 총 대기시간 요약, 진행 상태 표시가 없음.
   → 총 예상 소요시간 표시 + 실행 중인 작업을 보여주는 버튼 라벨 추가.

## 🟡 있으면 유용하지만 설계 변경이 좀 필요한 것

6. ✅ **실행 취소(정지) 기능 없음** — 백엔드는 `pendingTimeouts`로 재실행 시 이전 예약을 취소하지만,
   사용자가 직접 누르는 "중지" 버튼은 UI에 없음.
   → 실행 중 "중지" 버튼으로 남은 예약분 취소 가능.
7. ✅ **루틴 세트가 하나뿐** — "출근용"/"재택용"처럼 여러 작업 목록을 저장/전환하는 기능 없음.
   → 여러 루틴 세트(프로필)를 추가/전환/이름변경/삭제하고, 자동실행 세트를 별도로 지정 가능.
8. ✅ **드래그 앤 드롭 순서 변경 없음** — 위/아래 버튼으로 한 칸씩만 이동 가능.
   → 행을 드래그해서 임의 위치로 바로 재배치 가능(위/아래 버튼도 유지).

## 🟢 낮은 우선순위

- ✅ 설정 import/export(다른 PC로 백업/이전) 없음 → 전체 설정을 JSON 파일로 내보내기/가져오기 가능.
- ✅ 자동 실행(윈도우 부팅 시)이 조용히 실행돼서 사용자가 인지 못 할 수 있음 — 트레이 알림 필요
  → 자동 실행 시작 시 OS 알림 표시.
- ✅ 초 단위 딜레이만 지원 (5분 대기하려면 300 입력) → 입력에서 초/분 단위 전환 가능(저장은 계속 초 단위).

---

## 이후 추가로 처리한 것 (2026-08-01)

기능 개선 후보는 아니지만, 위 작업들을 하면서 같이 정리한 개발/배포 환경입니다.

- ✅ **배포용 실행 파일이 없었음** → electron-builder로 패키징 설정 (`npm run dist`).
  설치 파일(`DeskReady Setup 1.0.0.exe`)과 포터블 exe(`DeskReady 1.0.0.exe`) 생성.
- ✅ **패키징된 앱이 빈 화면으로 뜨던 버그** — Vite가 프로덕션 빌드에서 절대경로로 JS/CSS를
  참조해서 `file://`로 로드하는 Electron 환경에서 전혀 로드가 안 됐음 → `base: "./"` 설정으로 수정.
- ✅ **테스트가 전혀 없었음** → `react-app`에 Vitest 유닛 테스트(딜레이 포맷/단위 변환/재배치
  로직), 저장소 루트에 Playwright E2E 테스트(실제 Electron 앱을 띄워 삭제 확인·프로필·드래그·
  실행/중지·닫기 확인까지 10개 시나리오) 추가.
- ✅ **앱 아이콘이 기본값(Electron 기본 아이콘)이었음** → "모닝 노트" 색상의 커스텀 아이콘
  (모닝 커피잔 + 체크마크)으로 교체, 히어로 배너 워드마크도 같은 모티프로 통일.
- ✅ `npm audit fix`로 안전하게 고칠 수 있는 취약점 패치 (electron, vite). electron-builder/
  eslint 하위 의존성에 남은 것들은 `--force`가 필요해 보류 (devDependency라 배포 앱에는 영향 없음).
- ✅ `index.css`의 `@import` 순서 경고 수정.

## 다음 개선 후보 (2026-08-08 분석)

코드/의존성 리뷰에서 새로 발견한 항목들입니다. 아직 미구현.

### 보안

1. **가져오기(import-config) 시 작업 값 검증이 없음** — `import-config`는 JSON
   스키마만 확인(`profiles` 배열 존재 여부)하고 각 task의 `value`는 그대로
   신뢰함. `type: "program"`이고 `value`가 `"code "`로 시작하면 `main.js`가
   `exec()`(셸 실행)로 그대로 돌리므로, 출처를 모르는 config.json을 가져와
   실행하면 임의 셸 명령이 실행될 수 있음. "다른 PC로 설정 이전"(export/import)
   기능이 있어 신뢰 못 하는 파일을 공유받는 시나리오에서 실제 공격 경로가 됨.
   → 가져오기 시 최소 스키마 검증(허용된 `type`/필드 형태만 통과) 또는 가져오기
   화면에 "신뢰하는 파일만 가져오세요" 경고 추가.
2. **react-app 의존성에 high severity 취약점 2건** — `js-yaml`(CVE-2026-59870,
   쿼드러틱 CPU 소비), `nanoid`(size 0일 때 무한 루프). 둘 다 `npm audit fix`로
   해결 가능한 것으로 확인(루트 패키지는 0건). 빌드 도구 체인에만 걸리는
   devDependency일 가능성이 높지만 실제로 돌려서 확인 필요.

### 안정성

3. **config.json 손상 시 무경고 폴백** — `load-config`에서 JSON.parse가
   실패하면 조용히 빈 기본 설정을 반환함(디스크에 덮어쓰지는 않아 원본은
   남아있음). 사용자 입장에서는 루틴이 다 사라진 것처럼 보이고 왜 그런지 알
   방법이 없음. → 파싱 실패 시 렌더러에 알림("설정 파일을 읽지 못했습니다")
   추가.

### 기능

4. **자동 업데이트 없음** — `electron-builder`로 인스톨러만 만들고
   `electron-updater` 연동이 없어, 새 버전이 나와도 재설치가 필요함. 배포
   대상이 본인/소수라면 낮은 우선순위.
5. **CSP 미설정** — `react-app/index.html`에 Content-Security-Policy가 없음.
   지금은 전부 로컬 번들 콘텐츠만 로드해 실질 위험은 낮지만, Electron 보안
   체크리스트상 표준 권장 항목.
6. **자동실행 프로필이 요일/조건 구분 없이 1개 고정** — "출근용"/"재택용"
   프로필은 만들 수 있지만 자동실행 대상(`autoStartProfileId`)은 하나뿐이라,
   재택 요일엔 매번 수동으로 프로필을 바꿔야 함.
7. **항목(작업)별 자동실행 여부 ON/OFF** — 지금은 프로필 전체가 통째로
   실행되거나 안 되거나뿐이라, 특정 작업 하나만 오늘은 건너뛰고 싶어도
   삭제했다가 다시 추가하거나 값을 바꿔야 함. 각 task에 `enabled` 같은 플래그를
   둬서 목록엔 남기되 자동/수동 실행 시 건너뛸 수 있게 하면 좋을 듯. 설계 시
   고려할 점: 꺼진 작업이 누적 딜레이(`totalDelay`) 계산에 포함되는지 여부,
   UI에서 꺼진 항목을 어떻게 시각적으로 구분할지(회색 처리 등), "테스트 실행"
   버튼은 `enabled` 상태와 무관하게 항상 동작해야 하는지 등.

다음 개선 후보가 생기면 이 문서에 새로 추가해 주세요.
